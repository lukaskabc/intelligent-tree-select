export default {
  LABEL_KEY: "label",
  VALUE_KEY: "value",
  CHILDREN_KEY: "children",
  VALUE_MARGIN_X: "0.25rem",
};

export const EMPTY_ARRAY = Object.freeze([]);
export const EMPTY_SET = Object.freeze(new Set());
export const ALLOWED_ID_TYPES = ["string", "number", "boolean", "bigint"];
